[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openenv-support-triage"
version = "0.1.0"
description = "OpenEnv support triage submission scaffold"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["app"]
